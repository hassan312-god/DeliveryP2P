<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Configuration Supabase</title>
    <style>
        body { font-family: Arial, sans-serif; padding: 20px; }
        .test-result { margin: 10px 0; padding: 10px; border-radius: 5px; }
        .success { background: #d4edda; color: #155724; }
        .error { background: #f8d7da; color: #721c24; }
        .info { background: #d1ecf1; color: #0c5460; }
    </style>
</head>
<body>
    <h1>Test Configuration Supabase</h1>
    <div id="results"></div>

    <!-- CDN Supabase -->
    <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
    <script src="config.js"></script>
    <script src="js/services/supabase.js"></script>
    
    <script>
        function addResult(message, type = 'info') {
            const results = document.getElementById('results');
            const div = document.createElement('div');
            div.className = `test-result ${type}`;
            div.textContent = message;
            results.appendChild(div);
        }

        async function testSupabase() {
            addResult('Début du test Supabase...', 'info');
            
            try {
                // Test 1: Vérifier si Supabase est chargé
                if (typeof window.supabase !== 'undefined') {
                    addResult('✅ Supabase CDN chargé', 'success');
                } else {
                    addResult('❌ Supabase CDN non chargé', 'error');
                    return;
                }

                // Test 2: Vérifier la configuration
                addResult(`📋 URL Supabase: ${CONFIG.SUPABASE.URL}`, 'info');
                addResult(`📋 Clé anonyme: ${CONFIG.SUPABASE.ANON_KEY.substring(0, 20)}...`, 'info');

                // Test 3: Créer le client
                const client = window.supabase.createClient(
                    CONFIG.SUPABASE.URL,
                    CONFIG.SUPABASE.ANON_KEY
                );
                addResult('✅ Client Supabase créé', 'success');

                // Test 4: Tester la connexion
                const { data, error } = await client.from('profiles').select('count').limit(1);
                
                if (error) {
                    addResult(`❌ Erreur connexion: ${error.message}`, 'error');
                } else {
                    addResult('✅ Connexion Supabase réussie', 'success');
                }

                // Test 5: Vérifier les services
                if (window.SupabaseAuthService) {
                    addResult('✅ Service d\'authentification disponible', 'success');
                } else {
                    addResult('❌ Service d\'authentification non disponible', 'error');
                }

                if (window.supabaseClient) {
                    addResult('✅ Client global disponible', 'success');
                } else {
                    addResult('❌ Client global non disponible', 'error');
                }

            } catch (error) {
                addResult(`❌ Erreur: ${error.message}`, 'error');
            }
        }

        // Lancer le test quand la page est chargée
        document.addEventListener('DOMContentLoaded', testSupabase);
    </script>
</body>
</html> 